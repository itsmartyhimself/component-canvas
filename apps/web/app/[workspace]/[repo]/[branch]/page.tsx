import { AppShell } from "@/components/live/app-shell"

// TODO: ROADMAP §Sidebar — InstanceBreadcrumb wiring. Route params accepted but unused;
// AppShell renders the existing demo registry regardless of {workspace, repo, branch}.
export default function InstancePage() {
  return <AppShell />
}
