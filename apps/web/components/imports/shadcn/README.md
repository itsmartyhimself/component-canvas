# imports/shadcn

Base components from shadcn/ui. Never edit these. Never use directly in app code.

Wrap each base in a `components/live/*` component and apply all visual styling there via tokens from `app/globals.css` and `.type-*` classes. These primitives provide structure and a11y — nothing more.
