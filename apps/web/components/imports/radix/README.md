# imports/radix

Headless Radix primitives. Never edit these. Never use directly in app code.

Wrap each primitive in a `components/live/*` component and apply all visual styling there via tokens from `app/globals.css` and `.type-*` classes.
